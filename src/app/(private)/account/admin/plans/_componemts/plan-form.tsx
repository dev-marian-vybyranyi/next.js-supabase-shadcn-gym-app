interface PlanFormProps {
  formType?: "add" | "edit";
  initialValues?: any;
}

const PlanForm = ({ formType, initialValues }: PlanFormProps) => {
  return <div className="mt-7">PlanForm</div>;
};

export default PlanForm;
